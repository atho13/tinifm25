name: Build luci-app-tinyfm

permissions:
  contents: write

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 120

    strategy:
      fail-fast: false
      max-parallel: 1
      matrix:
        include:
          - name: SNAPSHOT
            # Link SDK Snapshot x86/64
            sdk_url: "https://downloads.openwrt.org"
            feeds_branch: ""
            artifact_ext: "apk"
            find_pattern: "luci-app-tinyfm*.apk"

          - name: 24.10.0
            # Link SDK Rilis 24.10 x86/64 (Pastikan versi rilis tersedia di server)
            sdk_url: "https://downloads.openwrt.org"
            feeds_branch: ";openwrt-24.10"
            artifact_ext: "apk"
            find_pattern: "luci-app-tinyfm*.apk"

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Extract version
      id: get_version
      run: |
        VERSION=$(grep PKG_VERSION luci-app-tinyfm/Makefile | sed 's/.*:=//' | tr -d ' ')
        RELEASE=$(grep PKG_RELEASE luci-app-tinyfm/Makefile | sed 's/.*:=//' | tr -d ' ')
        echo "FULL_VERSION=${VERSION}-r${RELEASE}" >> $GITHUB_ENV

    - name: Install Dependencies
      run: |
        sudo apt update
        sudo apt install -y build-essential clang flex bison g++ gawk \
          gcc-multilib g++-multilib gettext git libncurses-dev \
          libssl-dev python3-distutils rsync unzip zlib1g-dev \
          file wget curl xz-utils zstd

    - name: Download & Extract SDK
      run: |
        wget -q "${{ matrix.sdk_url }}" -O sdk.tar.zst
        tar --use-compress-program=unzstd -xf sdk.tar.zst
        EXTRACTED_DIR=$(ls -d openwrt-sdk-* | head -n1)
        mv "$EXTRACTED_DIR" sdk

    - name: Prepare SDK
      run: |
        cd sdk
        ./scripts/feeds update -a
        # Install dependensi PHP agar Makefile tidak error saat compile
        ./scripts/feeds install php8 php8-cgi php8-fpm php8-mod-session \
          php8-mod-ctype php8-mod-fileinfo php8-mod-zip php8-mod-iconv \
          php8-mod-mbstring coreutils zoneinfo-asia luci-base luci-compat
        
        cp -r ../luci-app-tinyfm package/
        echo "CONFIG_PACKAGE_luci-app-tinyfm=m" >> .config
        make defconfig

    - name: Compile Package
      run: |
        cd sdk
        make package/luci-app-tinyfm/compile V=s -j$(nproc)

    - name: Locate Artifact
      id: locate
      run: |
        # Cari di folder bin hasil compile
        FILE=$(find sdk/bin/packages -name "${{ matrix.find_pattern }}" | head -n1)
        if [ -z "$FILE" ]; then
          echo "ERROR: File tidak ditemukan!"
          find sdk/bin/packages -type f
          exit 1
        fi
        NEW_NAME="${{ matrix.name }}-$(basename $FILE)"
        mv "$FILE" "$NEW_NAME"
        echo "ARTIFACT_PATH=$NEW_NAME" >> $GITHUB_ENV

    - name: Publish to GitHub Release
      uses: softprops/action-gh-release@v2
      with:
        tag_name: ${{ startsWith(github.ref, 'refs/tags/') && github.ref_name || 'latest-build' }}
        name: ${{ startsWith(github.ref, 'refs/tags/') && github.ref_name || 'Latest Build' }}
        body: |
          **Informasi Build:**
          - Versi: `${{ env.FULL_VERSION }}`
          - Target: `x86/64`
          - Tanggal: ${{ github.event.repository.pushed_at }}
        files: ${{ env.ARTIFACT_PATH }}
        allow_updates: true
